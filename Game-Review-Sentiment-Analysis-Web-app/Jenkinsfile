pipeline {
    agent any

    parameters {
        booleanParam(name: 'KEEP_CONTAINERS_RUNNING', defaultValue: true, description: 'Keep Docker containers running after pipeline completion')
    }

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code...'
                checkout scm
            }
        }

        stage('Check Docker Version') {
            steps {
                echo 'Checking Docker and Docker Compose versions...'
                bat 'docker --version'
                bat 'docker compose version'
            }
        }

        stage('Check Docker Connectivity') {
            steps {
                echo 'Checking Docker Hub connectivity...'
                script {
                    try {
                        bat 'docker pull hello-world'
                        echo '✅ Docker Hub connectivity is working!'
                    } catch (Exception e) {
                        echo "⚠️  Docker Hub connectivity issue detected: ${e.getMessage()}"
                        echo 'This might cause build failures. Checking alternative approaches...'

                        // Try to check if images are already cached locally
                        bat 'docker images || echo "Cannot check local images"'

                        // Suggest solutions
                        echo 'Possible solutions:'
                        echo '1. Check your internet connection'
                        echo '2. Try using a different Docker registry mirror'
                        echo '3. Use locally cached images if available'
                        echo '4. Check firewall/proxy settings'
                    }
                }
            }
        }

        stage('Verify Docker Compose File') {
            steps {
                echo 'Checking if docker-compose.yml exists...'
                script {
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        if (fileExists('docker-compose.yml')) {
                            echo '✅ docker-compose.yml found!'
                            // Read and display basic info about the compose file
                            def composeContent = readFile 'docker-compose.yml'
                            echo 'Docker Compose file contents preview:'
                            // Show first few lines for verification
                            def lines = composeContent.split('\n')
                            for (int i = 0; i < Math.min(5, lines.size()); i++) {
                                echo "  ${lines[i]}"
                            }

                            // Check if Dockerfiles exist
                            if (fileExists('game-review-sentiment/Dockerfile')) {
                                echo '✅ Frontend Dockerfile found!'
                            } else {
                                echo '❌ Frontend Dockerfile missing!'
                            }

                            // Backend Dockerfile check commented out - only deploying frontend
                            // if (fileExists('game-review-sentiment-backend/Dockerfile')) {
                            //     echo '✅ Backend Dockerfile found!'
                            // } else {
                            //     echo '❌ Backend Dockerfile missing!'
                            // }
                        } else {
                            error '❌ docker-compose.yml not found! Pipeline cannot continue.'
                        }
                    }
                }
            }
        }

        stage('Run Hello World Service') {
            steps {
                echo 'Running Hello World service from docker-compose...'
                script {
                    // Change to the correct directory containing docker-compose.yml
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        bat 'docker compose run --rm hello-world'
                    }
                }
            }
        }

        stage('Build Frontend') {
            steps {
                echo 'Building frontend service...'
                script {
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        // Retry logic for Docker build to handle network issues
                        def maxRetries = 3
                        def retryCount = 0
                        def buildSuccess = false

                        while (retryCount < maxRetries && !buildSuccess) {
                            try {
                                echo "Attempt ${retryCount + 1} of ${maxRetries} to build frontend..."
                                bat 'docker compose build --no-cache frontend'
                                buildSuccess = true
                                echo '✅ Frontend build successful!'
                            } catch (Exception e) {
                                retryCount++
                                echo "❌ Frontend build attempt ${retryCount} failed: ${e.getMessage()}"

                                if (retryCount < maxRetries) {
                                    echo "⏳ Waiting 30 seconds before retry..."
                                    bat 'timeout /t 30 /nobreak > nul'

                                    // Clean up any partial builds
                                    bat 'docker system prune -f || echo "Cleanup failed, continuing..."'
                                } else {
                                    echo "❌ All ${maxRetries} attempts failed. Checking Docker connectivity..."
                                    // Try to diagnose the issue
                                    bat 'docker info || echo "Docker info failed"'
                                    bat 'docker pull hello-world || echo "Cannot reach Docker Hub"'
                                    error "Frontend build failed after ${maxRetries} attempts. Last error: ${e.getMessage()}"
                                }
                            }
                        }
                    }
                }
            }
        }

        // stage('Test Frontend') {
        //     steps {
        //         echo 'Running frontend tests...'
        //         script {
        //             dir('Game-Review-Sentiment-Analysis-Web-app') {
        //                 // Run frontend tests inside the container
        //                 bat 'docker compose run --rm frontend npm test -- --watchAll=false --passWithNoTests'
        //             }
        //         }
        //     }
        // }

        // stage('Build Backend (Optional)') {
        //     steps {
        //         echo 'Building backend service...'
        //         script {
        //             dir('Game-Review-Sentiment-Analysis-Web-app') {
        //                 // Retry logic for Docker build to handle network issues
        //                 def maxRetries = 3
        //                 def retryCount = 0
        //                 def buildSuccess = false

        //                 while (retryCount < maxRetries && !buildSuccess) {
        //                     try {
        //                         echo "Attempt ${retryCount + 1} of ${maxRetries} to build backend..."
        //                         bat 'docker compose build --no-cache backend'
        //                         buildSuccess = true
        //                         echo '✅ Backend build successful!'
        //                     } catch (Exception e) {
        //                         retryCount++
        //                         echo "❌ Backend build attempt ${retryCount} failed: ${e.getMessage()}"

        //                         if (retryCount < maxRetries) {
        //                             echo "⏳ Waiting 30 seconds before retry..."
        //                             bat 'timeout /t 30 /nobreak > nul'

        //                             // Clean up any partial builds
        //                             bat 'docker system prune -f || echo "Cleanup failed, continuing..."'
        //                         } else {
        //                             echo "❌ All ${maxRetries} attempts failed."
        //                             error "Backend build failed after ${maxRetries} attempts. Last error: ${e.getMessage()}"
        //                         }
        //                     }
        //                 }
        //             }
        //         }
        //     }
        // }

        stage('Test Services') {
            steps {
                echo 'Testing frontend service...'
                script {
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        // Start frontend service in detached mode for testing
                        bat 'docker compose up -d frontend'
                        
                        // Wait a moment for service to start
                        bat 'timeout /t 15 /nobreak > nul'
                        
                        // Check if frontend service is running
                        bat 'docker compose ps'
                        
                        // Test hello-world service
                        echo 'Testing hello-world service...'
                        bat 'docker compose run --rm hello-world'
                        
                        // Stop services after testing
                        bat 'docker compose down'
                    }
                }
            }
        }

        stage('Deploy Frontend Locally') {
            steps {
                echo '🚀 Deploying frontend application locally...'
                script {
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        // Stop any existing containers
                        bat 'docker compose down || echo "No existing containers to stop"'
                        
                        // Start frontend service in detached mode
                        bat 'docker compose up -d frontend'
                        
                        // Wait for service to be ready
                        // bat 'timeout /t 20 /nobreak > nul'
                        
                        // Verify frontend service is running
                        bat 'docker compose ps'
                        
                        echo '✅ Frontend deployed successfully!'
                        echo '🌐 Frontend: http://localhost:3000'
                    }
                }
            }
        }

        stage('Verify Frontend Deployment') {
            steps {
                echo '🔍 Verifying frontend deployment accessibility...'
                script {
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        // Test if frontend port is accessible
                        echo 'Testing port accessibility...'
                        
                        // Check if frontend port is listening
                        bat 'netstat -an | find "3000" || echo "Port 3000 not found in netstat"'
                        
                        // Try to curl the frontend (if curl is available)
                        bat 'curl -f http://localhost:3000 || echo "Frontend not responding on port 3000"'
                        
                        echo '✅ Frontend deployment verification completed!'
                        echo '🌐 You can now access your frontend at: http://localhost:3000'
                    }
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed.'
            script {
                // Only clean up if the parameter is set to false
                if (!params.KEEP_CONTAINERS_RUNNING) {
                    echo '🧹 Cleaning up Docker containers...'
                    dir('Game-Review-Sentiment-Analysis-Web-app') {
                        bat 'docker compose down || echo "No containers to clean up"'
                    }
                } else {
                    echo '📦 Keeping containers running as requested...'
                }
            }
        }
        success {
            echo '🎉 Pipeline succeeded!'
            script {
                if (params.KEEP_CONTAINERS_RUNNING) {
                    echo 'Your frontend application is deployed and running locally!'
                    echo ''
                    echo '🌐 Access your frontend application:'
                    echo '   Frontend (React): http://localhost:3000'
                    echo ''
                    echo 'To stop the application manually:'
                    echo '   cd Game-Review-Sentiment-Analysis-Web-app'
                    echo '   docker compose down'
                    echo ''
                    echo 'To restart:'
                    echo '   docker compose up -d frontend'
                } else {
                    echo 'Pipeline completed successfully. Containers have been cleaned up.'
                }
            }
        }
        failure {
            echo '❌ Pipeline failed!'
            script {
                def failureCause = currentBuild.getResult()
                if (failureCause == 'FAILURE') {
                    echo '🔍 Troubleshooting tips for TLS timeout issues:'
                    echo '1. Check your internet connection'
                    echo '2. Try restarting Docker Desktop'
                    echo '3. Check firewall/proxy settings'
                    echo '4. Try using a VPN if network is restricted'
                    echo '5. Consider using a different Docker registry mirror'
                    echo '6. Check if Docker Hub is accessible: ping registry-1.docker.io'
                }
                // Always clean up on failure
                echo '🧹 Cleaning up containers due to failure...'
                dir('Game-Review-Sentiment-Analysis-Web-app') {
                    bat 'docker compose down || echo "No containers to clean up"'
                }
            }
        }
    }
}
